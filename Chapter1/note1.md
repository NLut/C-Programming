##### A Tutorial Intruduction

#### Symbolic Constant:
    #define name replacement txt
    - Not variable.
    - Conventionally write in *UPPERCASE*
   
#### Character Input and Output:
    - Text Stream:
        - A sequence of characters divided into line which end with \n.
    - > getchar:
        - read next input character.
    - > putchar(c):
        - print the contets of the integer. variable c as a character.
        - can print only 1 char at a time.

### File Copying:
    - EOF: end of file
        - integer defined in <stdio.h>
        - #define EOF (-1)
    - "%ld" is place holder for long.
    
### Word Count:
    - nl = nw = nc = 0; equal to nl = (nw = (nc = 0));
    - assign value return it's value.
